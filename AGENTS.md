# Bio Calculator contributor map

Read `docs/architecture.md`, `docs/calculation-model.md`, and `docs/evidence-ledger.md` before changing calculations. SQL is canonical for nutrition reference data; TypeScript contains types and pure calculation logic, never a second nutrient registry. Preserve provenance, uncertainty, and the distinction between facts and models.
