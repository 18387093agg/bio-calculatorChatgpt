import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';
const migrations=['supabase/migrations/202609230001_normalized_nutrition.sql','supabase/migrations/202609230002_complete_canonical_pipeline.sql','supabase/migrations/202609250003_scientific_audit_and_integrity.sql','supabase/migrations/202609250004_schema_completeness.sql'].map(x=>readFileSync(x,'utf8')).join('\n');
test('schema represents all required entities and protects personal rows',()=>{for(const table of ['foods','nutrient_forms','food_nutrients','preparation_methods','food_preparation_profiles','retention_factors','evidence_sources','targets','target_model_rules','model_assumptions','interactions','supplements','supplement_components','user_profiles','meal_logs','meal_items','biomarker_results','biomarker_reference_ranges','biomarker_interpretation_rules','user_target_overrides','food_exclusions'])assert.match(migrations,new RegExp(`create table ${table}`));for(const personal of ['user_profiles','meal_logs','meal_items','biomarker_results','user_target_overrides','supplement_logs','food_exclusions'])assert.match(migrations,new RegExp(`alter table ${personal} enable row level security`));});
test('schema validates nutrient units, provenance, external duplicates, and target ranges',()=>{assert.match(migrations,/food_nutrients_unit_check/);assert.match(migrations,/food_nutrients_provenance_check/);assert.match(migrations,/foods_external_identity/);assert.match(migrations,/override_range_order/);});
