# Optional. The public demo works without these values.
# Use the anonymous browser key only; never put a Supabase service-role key here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional HTTP port for `npm run dev` and `npm start`.
PORT=3000
